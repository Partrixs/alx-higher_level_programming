#!/bin/bash/python3
def magic_calculation(a, b):
    result = 0
    for it in range(1, 3):
        try:
            if it > a:
                raise Exception:
            result += b + a
        except Exception:
            break
    return result
